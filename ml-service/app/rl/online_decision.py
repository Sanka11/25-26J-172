# from app.rl.rl_inference import load_rl_agent
# from app.rl.online_state import build_online_state
# from app.rl.config import ACTIONS

# agent = load_rl_agent()

# def decide_action(gru_output, student_features):
#     state = build_online_state(gru_output, student_features)
#     action_id = agent.choose_action(state)
#     return ACTIONS[action_id]
